# OSM Data

Preprocessed OpenStreetMap road-network extracts used to build the
in-memory graph (`models.Graph`). Raw `.osm`/`.pbf` files are not
committed to the repository; this directory holds the preprocessed,
already-parsed output the Phase 6 Graph Loader consumes.
